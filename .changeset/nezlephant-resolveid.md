---
"envapt": patch
---

Add an optional `resolveId` hook to the `envapt-nezlephant` plugin.

This is a small, fully backward-compatible change that allows resolving `nez:id:...` tokens via a user-provided function.
