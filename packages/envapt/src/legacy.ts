/**
 * The legacy decorator entry (`envapt/legacy`). Exports the experimental-decorator form of `@Envapt`
 * and the sugar decorators for projects using `experimentalDecorators: true`.
 *
 * @module
 */

export * from './decorators/legacy';
export type { EnvaptFieldDecorator } from './types';
