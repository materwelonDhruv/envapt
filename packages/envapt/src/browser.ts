/**
 * The browser build entry (`envapt/browser`). Re-exports the portable surface with no `node:*`
 * dependencies. Supply a source like `ManualEnvSource` before reading.
 *
 * @module
 */

export * from './index.portable';
